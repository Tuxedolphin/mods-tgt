info:
  name: put /profile/me
  type: http
  seq: 2
  tags:
    - Profile

http:
  method: PUT
  url: "{{baseUrl}}/profile/me"
  body:
    type: json
    data: |-
      {
        "username": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/profile/me"
      method: PUT
      body:
        type: json
        data: |-
          {
            "username": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
