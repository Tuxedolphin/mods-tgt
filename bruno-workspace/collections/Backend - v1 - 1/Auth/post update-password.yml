info:
  name: post update-password
  type: http
  seq: 8
  tags:
    - Auth
  docs: Resets the user's password using a one-time recovery token.

http:
  method: POST
  url: "{{baseUrl}}/auth/update-password"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "oldPassword": "",
        "newPassword": ""
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
