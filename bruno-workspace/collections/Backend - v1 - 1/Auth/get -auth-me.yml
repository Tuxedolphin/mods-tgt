info:
  name: get /auth/me
  type: http
  seq: 5
  tags:
    - Auth

http:
  method: GET
  url: "{{baseUrl}}/auth/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/auth/me"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
