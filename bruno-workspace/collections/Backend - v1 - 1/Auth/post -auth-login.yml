info:
  name: post /auth/login
  type: http
  seq: 2
  tags:
    - Auth

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "zhuzhen.zhuo@gmail.com",
        "password": "testabd123"
      }

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:5233
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setEnvVar("accessToken", data.accessToken, { persist: false });
        bru.setEnvVar("refreshToken", data.refreshToken, { persist: false });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/auth/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "",
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: |-
          {
            "accessToken": "",
            "refreshToken": "",
            "expiresIn": "",
            "tokenType": ""
          }
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/auth/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "",
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "accessToken": "",
            "refreshToken": "",
            "expiresIn": "",
            "tokenType": ""
          }
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/auth/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "",
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: |-
          {
            "accessToken": "",
            "refreshToken": "",
            "expiresIn": "",
            "tokenType": ""
          }
