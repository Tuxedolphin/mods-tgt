info:
  name: delete /timetable/:id
  type: http
  seq: 6
  tags:
    - Timetable

http:
  method: DELETE
  url: "{{baseUrl}}/timetable/:id"
  params:
    - name: id
      value: "{{timetableId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Timetable deleted
    description: Deletes a timetable owned by the authenticated user.
    request:
      url: "{{baseUrl}}/timetable/:id"
      method: DELETE
      params:
        - name: id
          value: 2c1f2f7e-fcb6-4a8d-8465-cf6d924b6133
          type: path
    response:
      status: 204
      statusText: No Content
